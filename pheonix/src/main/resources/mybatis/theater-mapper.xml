<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="theater">
	
	<select id="sequence" resultType="int">
		select theater_seq.nextval from dual
	</select>
	
	<insert id="insert">
		insert into theater (theater_no, cinema_no, theater_name, theater_total_seats) 
		values (#{theaterNo}, #{cinemaNo}, #{theaterName}, #{theaterTotalSeats})
	</insert>
	
	<update id="update">
		update theater
		<set>
			<if test="theaterName !=null">theater_name = ${theaterName}, </if>
			<if test="theaterTotalSeats !=null">theater_total_seats = ${theaterTotalSeats}, </if>
		</set>
		where theater_no = #{theaterNo}
	</update>
	
	<select id="list" resultType="TheaterDto">
		select * from theater order by theater_no asc
	</select>
	
	<select id="find" resultType="TheaterDto">
		select * from theater where cinema_no = #{cinemaNo}
	</select>
	
	<delete id="delete">
		delete theater where theater_no = #{theaterNo}
	</delete>

</mapper>