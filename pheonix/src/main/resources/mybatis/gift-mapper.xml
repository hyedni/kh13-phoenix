<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="gift">
	
	<!-- 데이터 삽입 -->
	<select id="giftSequence" resultType="int">
		select gift_seq.nextval from dual
	</select>
	<insert id="giftAdd">
		insert into gift(
			gift_no, gift_user_id, gift_purchase_no,
			gift_to, gift_email, gift_content
		)
		value(
			#{giftNo}, #{giftUserId}, #{giftPurchaseNo},
			#{giftTo}, #{giftEmail}, #{giftContent}
		)
	</insert>
	
	<!-- 데이터 조회 -->
	<!-- 전체 조회 -->
	<select id="giftList" resultType="GiftDto">
		select * from gift order by gift_no asc
	</select>
	
</mapper>